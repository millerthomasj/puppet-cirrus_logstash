input {
  tcp {
    type => syslog
    port => <%= scope.lookupvar('cirrus_logstash::syslog_port') %>
    add_field => { '[metrics][logstash_host]' => '<%= scope.lookupvar('::fqdn') %>' }
  }
  udp {
    type => syslog
    port => <%= scope.lookupvar('cirrus_logstash::syslog_port') %>
    add_field => { '[metrics][logstash_host]' => '<%= scope.lookupvar('::fqdn') %>' }
  }
}
