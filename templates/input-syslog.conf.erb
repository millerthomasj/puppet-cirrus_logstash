input {
  tcp {
    type => syslog
    port => <%= @syslog_port %>
    add_field => { '[metrics][logstash_host]' => '<%= @fqdn %>' }
  }
  udp {
    type => syslog
    port => <%= @syslog_port %>
    add_field => { '[metrics][logstash_host]' => '<%= @fqdn %>' }
  }
}
